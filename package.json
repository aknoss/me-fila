{
  "name": "me-fila",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build": "npm run build -w client && npm run migrate -w server",
    "start": "npm run start -w server",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:docker": "docker compose -f docker-compose.test.yml up -d --wait && npx playwright test; EXIT_CODE=$?; docker compose -f docker-compose.test.yml down -v; exit $EXIT_CODE"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "concurrently": "^9.2.1",
    "knex": "^3.3.0",
    "mysql2": "^3.24.2"
  }
}
